import 'package:flutter/material.dart';

class OperationsManagerHomeScreen extends StatelessWidget {
  const OperationsManagerHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Trang chủ Quản lý hoạt động'),
    );
  }
}
